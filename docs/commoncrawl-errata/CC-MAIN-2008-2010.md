- Index timestamps are not UTC but Pacific Time (America/Los_Angeles), need to
  be fixed
- 302/301 redirects were followed but the capture data still has the
  pre-redirect url --> difficult to fix but for e.g. cases where the url does
  not end with a slash but the requested url did, this can be fixed probably
- When extracting headers, need to filter out any with the "x_commoncrawl_"
  prefix (2008-2010) as they are not original but added by common crawl. There
  are also some headers with the "x-commoncrawl-" prefix (e.g. if truncated)
